<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <!-- <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1"> -->
  <title>Wanderlust</title>
  <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <link rel="stylesheet" href="/css/materialize.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav>
    <div class="nav-wrapper">
      <a href="/" class="brand-logo">Wanderlust</a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <li><%= link_to "About", about_path %></li>
        <li><%= link_to "Places", places_path %></li>
        <li><a href="#">My Bucket List</a></li>
        <li><a href="#">My Profile</a></li>
        <li><%# link_to "My Bucket List", user_my_places_list(current_user) %></li>
        <li><% link_to "My Profile", user_path(current_user) %></li>
      </ul>
    </div>
  </nav>
  <% if current_user %>
    <div class="user-menu"
    <h6>Hi,<%= current_user.username %>! <%# link_to "My Profile", user_path(@user) %>
    <%= link_to "Logout", logout_path, method: :delete %></h6>
    </div>
  <% else %>
    <div class="before-user-menu">
    <%= link_to "Login", login_path %> | <%= link_to "Sign in", new_user_path %>
    </div>
  <% end %>
  <main>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: name %>
    <% end %>
    <%= yield %>
  </main>
  <footer>
  </footer>
  <script src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
  <script src="/js/materialize.js"></script>
</body>
</html>
